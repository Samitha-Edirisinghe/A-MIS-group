
  # A'MIS group

  This is a code bundle for A'MIS group. The original project is available at https://www.figma.com/design/gSapaXW8UBhb1bx10jOtQV/A-MIS-group.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  